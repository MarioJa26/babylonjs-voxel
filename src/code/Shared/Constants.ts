// ---------------------------------------------------------------------------
// Constants
//
// Shared constants that are referenced across multiple modules. Extracted to
// break import cycles (e.g. BlockRaycaster → Player just for REACH_DISTANCE).
// ---------------------------------------------------------------------------

/** Maximum reach distance for block interaction (blocks). */
export const REACH_DISTANCE = 64;
